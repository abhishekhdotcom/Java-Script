<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Verify Your Email</title>
        <script src="https://cdn.tailwindcss.com"></script>
    </head>

    <body class="bg-gray-100 flex  flex-col items-center  min-h-screen">
        <%- include('partials/header') %>

            <div class="bg-white p-8 rounded-xl shadow-2xl w-full max-w-md mt-10">
                <div class="text-center">
                    <h2 class="text-3xl font-bold text-gray-800 mb-6">Verify Your Email</h2>
                    <div class="bg-green-100 text-green-800 p-3 rounded-lg inline-block mb-6">
                        <span class="flex items-center justify-center">
                            <svg class="w-6 h-6 mr-2" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd"
                                    d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z"
                                    clip-rule="evenodd" />
                            </svg>
                            You are logged in
                        </span>
                    </div>
                </div>
                <div class="mb-6">
                    <p class="text-gray-700 text-center">Email: <span class="font-semibold">
                            <%= user.email %>
                        </span></p>
                    <p class="text-gray-600 text-center mt-2">Your email has not been verified yet. Please verify your
                        email
                        by entering the 8-digit code or request a new verification link.</p>
                </div>
                <form action="/api/v1/verify/resend-email-verify-link" method="POST">
                    <button type="submit"
                        class="bg-gray-200 text-gray-800 px-6 py-2 rounded-lg w-full mb-6 hover:bg-gray-300 transition">Resend
                        Verification Link</button>
                </form>
                <div>
                    <form action="/api/v1/verify/verify-email-token" method="GET">
                        <label class="block text-gray-700 mb-2 text-center" for="verifyEmailToken">Enter 8-Digit
                            Verification
                            Code:</label>
                        <input type="text" id="verifyEmailToken" name="emailToken"
                            class="w-full p-3 border border-gray-300 rounded-lg mb-4 focus:outline-none focus:ring-2 focus:ring-indigo-500"
                            maxlength="8" placeholder="e.g., 12345678">

                        <input type="hidden" value="<%= user.email %>" name="verifyEmail">

                        <button type="submit"
                            class="bg-indigo-600 text-white px-6 py-2 rounded-lg w-full hover:bg-indigo-700 transition">Verify
                            Code</button>
                    </form>
                </div>
            </div>
    </body>

</html>