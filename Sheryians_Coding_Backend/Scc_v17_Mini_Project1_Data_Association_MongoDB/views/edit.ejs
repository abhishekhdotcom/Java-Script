<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <title>Edit Post - B4Blog</title>
        <!-- Tailwind CSS -->
        <link rel="stylesheet" href="/stylesheets/output.css" />
        <!-- Font Awesome for icons -->
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" />
    </head>

    <body class="bg-gradient-to-r from-blue-600 to-purple-600 min-h-screen text-white font-sans antialiased">
        <!-- Navigation Bar -->
        <nav class="bg-purple-800 sticky top-0 z-30 p-2 shadow-lg flex justify-between items-center px-5">
            <h1 class="md:text-3xl text-2xl font-extrabold">B4Blog</h1>
            <a href="/logout"
                class="flex items-center bg-red-600 hover:bg-red-700 px-4 py-1.5 rounded-xl transition transform hover:scale-103 shadow-md">
                <i class="fas fa-sign-out-alt mr-2"></i> Logout
            </a>
        </nav>
        <!-- main content -->
        <main class="container mx-auto md:p-6 p-2 flex justify-center">

            <section
                class="bg-white bg-opacity-90 rounded-2xl md:p-3 lg:p-6 p-4 shadow-lg text-gray-900 max-w-4xl w-full lg:mt-2 md:mt-0 mt-2">
                <h2 class="text-2xl md:text-3xl font-bold text-indigo-800 mb-4 text-center">Edit Post</h2>
                <form action="/post/<%= post._id %>/update?_method=PUT" method="POST" class="flex flex-col gap-3">
                    <div>
                        <label for="category" class="block text-sm font-medium text-gray-700 mb-2">Category</label>
                        <select id="category" name="category" required
                            class="w-full p-3 rounded-md border border-gray-300 focus:outline-indigo-500 text-gray-900">
                            <option value="" disabled>Select a category</option>
                            <option value="Tech" <%=post.category==='Tech' ? 'selected' : '' %>>Tech</option>
                            <option value="Food" <%=post.category==='Food' ? 'selected' : '' %>>Food</option>
                            <option value="Software" <%=post.category==='Software' ? 'selected' : '' %>>Software
                            </option>
                            <option value="Lifestyle" <%=post.category==='Lifestyle' ? 'selected' : '' %>>Lifestyle
                            </option>
                            <option value="Travel" <%=post.category==='Travel' ? 'selected' : '' %>>Travel</option>
                            <option value="Health" <%=post.category==='Health' ? 'selected' : '' %>>Health</option>
                            <option value="Education" <%=post.category==='Education' ? 'selected' : '' %>>Education
                            </option>
                            <option value="Others" <%=post.category==='Others' ? 'selected' : '' %>>Others</option>
                        </select>
                    </div>
                    <div class="flex-1">
                        <label for="postTitle" class="block text-sm font-medium text-gray-700 mb-2">PostTitle</label>
                        <input type="text" name="postTitle" id="postTitle" placeholder="Post Title" required
                            value="<%= post.postTitle%>" maxlength="100"
                            class="w-full p-3 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-indigo-500 text-gray-900 transition bg-white placeholder-gray-500" />
                    </div>
                    <div>
                        <label for="content" class="block text-sm font-medium text-gray-700 mb-2">Content</label>
                        <textarea id="content" name="content" rows="10" required
                            class="w-full p-3 rounded-md border border-gray-300 focus:outline-indigo-500 resize-none text-gray-900"
                            placeholder="Update your post..."><%= post.content %></textarea>
                    </div>
                    <div class="flex gap-4">
                        <button type="submit"
                            class="flex-1 bg-indigo-600 hover:bg-indigo-700 text-white py-2 rounded-xl font-semibold transition transform duration-300 cursor-pointer hover:scale-102">
                            <i class="fas fa-save mr-2"></i> Update Post
                        </button>
                        <a href="/profile" onclick="return confirm('Discard changes?')"
                            class="flex-1 bg-gray-500 hover:bg-gray-600 text-white py-2 rounded-xl font-semibold transition transform duration-300 cursor-pointer hover:scale-102 text-center">
                            <i class="fas fa-arrow-left mr-2"></i> Cancel
                        </a>
                    </div>
                </form>
            </section>
        </main>
    </body>

</html>